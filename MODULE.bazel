module(
    name = "build_athena_bazel_platforms",
    version = "1.0.1",  # keep in sync with version.bzl
    compatibility_level = 1,
)

bazel_dep(name="build_athena_bazel_platforms" version="1.0.1")
